/* exported defaults */
// function defaults(target, source) {
//   var targetProperties = Object.keys(target);
//   var targetValues = Object.values(target);
//   var sourceProperties = Object.keys(target);
//   var sourceValues = Object.values(source);
//   var i;
//   for (i = 0; i < sourceProperties.length; i++) {
//     console.log(sourceProperties)
//     if (targetProperties.includes(sourceProperties[i])) {
//       sourceProperties.splice(i, 1);
//       console.log(sourceProperties)
//     }
//   }

// }

// function defaults(target, source) {
//   var key;
//   for (key in source) {
//     if (!target.hasOwnProperty(key)) {
//       target[key] = source[key]
//     }
//   }
// }
